# dbgate/.env.template
# This file is primarily for documenting variables used by DBGate.
# DBGate's docker-compose.yml is pre-configured to use global environment
# variables (from the root .env file) for its default PostgreSQL and MongoDB connections.

# --- Informational: Variables DBGate uses from the root .env ---
# These are typically set in the project's root .env file.
# POSTGRES_HOST=postgres
# POSTGRES_USER=your_postgres_user
# POSTGRES_PASSWORD=your_strong_postgres_password_here
# POSTGRES_PORT=5432

# MONGODB_HOST=mongodb
# MONGODB_ROOT_USER=root # DBGate example uses root for MongoDB
# MONGODB_ROOT_PASSWORD=your_strong_mongodb_root_password_here
# MONGODB_PORT=27017

# --- Optional: Overriding or Adding Connections ---
# If you wish to define connections differently or add more than the defaults
# configured in docker-compose.yml, you can create a dbgate/.env file
# and define variables here. These would typically override or augment
# the 'environment' section in dbgate/docker-compose.yml if that file
# also includes `env_file: .env`.
# Note: The current dbgate/docker-compose.yml does NOT load a service-specific .env file.
# To use variables from a dbgate/.env, you would need to add `env_file: .env`
# to its docker-compose.yml service definition.

# Example for defining additional connections if you modify docker-compose.yml's
# 'CONNECTIONS' environment variable or use a local .env with env_file loading:
#
# CONNECTIONS=mypg_override,myredis,another_mongo
#
# # PostgreSQL Connection Override Example
# LABEL_mypg_override=Custom PostgreSQL
# SERVER_mypg_override=custom-postgres-host
# USER_mypg_override=custom_user
# PASSWORD_mypg_override=custom_password
# PORT_mypg_override=5432
# ENGINE_mypg_override=postgres@dbgate-plugin-postgres
#
# # Redis Connection Example
# LABEL_myredis=Project Redis Cache
# SERVER_myredis=redis # Assuming a shared Redis service
# PORT_myredis=6379
# # PASSWORD_myredis=your_redis_password_if_any # Add if Redis requires auth
# ENGINE_myredis=redis@dbgate-plugin-redis
#
# # Another MongoDB Connection Example
# LABEL_another_mongo=Legacy MongoDB
# SERVER_another_mongo=old-mongo-server
# USER_another_mongo=legacy_user
# PASSWORD_another_mongo=legacy_pass
# PORT_another_mongo=27017
# ENGINE_another_mongo=mongo@dbgate-plugin-mongo

# For a comprehensive list of supported engines and their connection parameters,
# refer to the official DBGate documentation.
