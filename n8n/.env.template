# onestack/n8n/.env.template
# Copy this file to .env and fill in your actual values

# -- n8n Authentication --
N8N_BASIC_AUTH_ACTIVE=true
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=your_secure_password

# -- n8n Security --
# Generate a 32-character encryption key (important for credentials security)
N8N_ENCRYPTION_KEY=your_super_secret_encryption_key_32_chars_long

# -- n8n Configuration --
N8N_TIMEZONE=UTC

# -- Database Configuration --
# Database name for n8n (already defined in root .env as POSTGRES_N8N_DB=n8n)
# The following are inherited from the root .env file:
# - POSTGRES_HOST (postgres service)
# - POSTGRES_USER (database user)
# - POSTGRES_PASSWORD (database password)
# - POSTGRES_PORT (5432)
# - POSTGRES_N8N_DB (n8n database name)

# -- Queue Configuration --
# Redis configuration is inherited from the root .env file:
# - REDIS_HOST (redis service)
# - REDIS_PASSWORD (redis password)
# - REDIS_PORT (6379)

# -- Web Configuration --
# Domain configuration is inherited from the root .env file:
# - BASE_DOMAIN (your domain)
# - WEB_NETWORK_NAME (web network)
# - INTERNAL_NETWORK_NAME (internal network)

# TODO: Fill in the actual values for your n8n configuration
# 1. Change N8N_BASIC_AUTH_PASSWORD to a secure password
# 2. Generate a 32-character encryption key for N8N_ENCRYPTION_KEY
# 3. Adjust N8N_TIMEZONE if needed (e.g., America/New_York, Europe/London)
# 4. Make sure the shared postgres and redis services are running

# Note: n8n will automatically create the database tables on first run