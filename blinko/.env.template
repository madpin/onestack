## Blinko Service Environment Template
# Copy to .env and fill in secrets. Non-secret values can be left as defaults.

# Strong random secret (required). Generate with:
#   openssl rand -base64 32
NEXTAUTH_SECRET=

# Database name dedicated for Blinko (must exist in shared Postgres).
# Add `CREATE DATABASE blinko;` to shared/postgres/config/initdb/01-create.sql if not present.
BLINKO_DB_NAME=blinko

# Host port to expose (internally always 1111)
BLINKO_PORT=1111

# (Optional) Override base domain pieces – usually inherited from root .env
# BASE_DOMAIN=example.com

# Nothing else is strictly required; DATABASE_URL is assembled in docker-compose.yml


